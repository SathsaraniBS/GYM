# .env  (project root — same folder as package.json)
# ⚠️ Add this file to .gitignore — never commit it

# Development — your local backend
VITE_API_URL=http://localhost:5000

# When you deploy, change this to your live server URL, e.g.:
# VITE_API_URL=https://api.yourgymdomain.com